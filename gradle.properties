# Sets default memory used for gradle commands. Can be overridden by user or command line properties.
org.gradle.jvmargs=-Xmx1G
org.gradle.daemon=true
# Force the Gradle daemon to use JDK 17 (matches the mod's toolchain requirement).
# ForgeGradle 6 picks its bootstrap launcher based on the daemon JVM version: with JDK 21+
# it switches to net.neoforged.devlaunch.Main (DevLaunch) but fails to add DevLaunch.jar to
# the JavaExec classpath, causing ClassNotFoundException at launch.  Pinning to JDK 17 keeps
# ForgeGradle on the cpw.mods.bootstraplauncher.BootstrapLauncher path that works correctly.
org.gradle.java.home=C:/Users/natha/.gradle/jdks/eclipse_adoptium-17-amd64-windows.2
org.gradle.parallel=true
org.gradle.caching=true
org.gradle.configuration-cache=false

#read more on this at https://github.com/neoforged/ModDevGradle?tab=readme-ov-file#better-minecraft-parameter-names--javadoc-parchment
# you can also find the latest versions at: https://parchmentmc.org/docs/getting-started
parchment_minecraft_version=1.20.1
parchment_mappings_version=2023.09.03
# Environment Properties
# You can find the latest versions here: https://projects.neoforged.net/neoforged/neoforge
# The Minecraft version must agree with the Neo version to get a valid artifact
minecraft_version=1.20.1
# The Minecraft version range can use any release version of Minecraft as bounds.
# Snapshots, pre-releases, and release candidates are not guaranteed to sort properly
# as they do not follow standard versioning conventions.
minecraft_version_range=[1.20.1,1.21)
# NeoForge 1.20.1 uses net.neoforged:forge:1.20.1-47.x.x (net.minecraftforge.* namespace)
# net.neoforged:neoforge (net.neoforged.* namespace) only starts from 1.20.2 (20.2.x)
# TODO: All net.neoforged.* imports in Java sources must be replaced with net.minecraftforge.*
#       equivalents before this module can compile against Forge 1.20.1.
forge_version=47.4.10
# The loader version range can only use the major version of FML as bounds
loader_version_range=[47,)

## Mod Properties

# The unique mod identifier for the mod. Must be lowercase in English locale. Must fit the regex [a-z][a-z0-9_]{1,63}
# Must match the String constant located in the main mod class annotated with @Mod.
mod_id=fangs_n_claws
# The human-readable display name for the mod.
mod_name=Fangs 'n Claws
# The license of the mod. Review your options at https://choosealicense.com/. All Rights Reserved is the default.
mod_license=MIT
# The mod version. See https://semver.org/
mod_version=1.2.0
# The group ID for the mod. It is only important when publishing as an artifact to a Maven repository.
# This should match the base package used for the mod sources.
# See https://maven.apache.org/guides/mini/guide-naming-conventions.html
mod_group_id=net.raptorzizi.fangs_n_claws

geckolib_version=4.4.4
jei_mc_version=1.20.1
jei_version=15.20.0.105
